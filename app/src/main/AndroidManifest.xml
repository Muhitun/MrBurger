<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sohan.mrburger">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="com.example.sohan.mrburger.MainActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BurgerSellActivity"
            android:label="@string/title_activity_burger_sell"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Owner"
            android:label="@string/title_activity_owner"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Employee"
            android:label="@string/title_activity_employee"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OwnerWindow"
            android:label="@string/title_activity_owner_window"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>
